# Sensitive configuration files (DO NOT COMMIT)
# config/db.php (Router is safe to commit)
config/db_old_backup.php
config/db_enhanced.php
config/email.php
config/config.php

# Environment files
.env
.env.local
.env.production

# Test files (remove before deployment)
test_*.php
integration_test.php
run_tests.php
deployment_check.php
test_results_*.json

# Example files are safe to commit
# .env.example
# config/config.example.php
# config/email.example.php

# User uploaded content
uploads/profiles/*
uploads/shops/*
!uploads/profiles/.gitkeep
!uploads/shops/.gitkeep

# Composer dependencies (optional - uncomment if using composer)
vendor/
composer.lock

# System files
.DS_Store
Thumbs.db
*.log
*.tmp
.vscode/
.idea/

# Cache and temporary files
cache/
temp/
tmp/

# Node modules (if using any frontend build tools)
node_modules/
package-lock.json

# Database backups
*.sql
*.sql.gz
database/backups/

# PHPMailer (if installed via composer, already in vendor/)
# If manually installed, add the path here
